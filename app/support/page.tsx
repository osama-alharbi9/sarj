import { SupportDemo } from "@/components/support/SupportDemo";

/** "/support" — the same assistant, pointed at Najm's customer service door. */
export default function SupportPage() {
  return <SupportDemo />;
}

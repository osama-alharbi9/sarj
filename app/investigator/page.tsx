import { InvestigatorWorkspace } from "@/components/investigator/Workspace";

/** "/investigator" — the desk where fault actually gets determined. */
export default function InvestigatorPage() {
  return <InvestigatorWorkspace />;
}

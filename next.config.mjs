/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // This runs on a projector in front of a client — no dev overlay on screen.
  devIndicators: false,
};

export default nextConfig;
